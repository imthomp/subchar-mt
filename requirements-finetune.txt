# Supercomputer / fine-tuning dependencies (finetune_experiment.py)
# Install PyTorch first with the appropriate CUDA version, e.g.:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# Then install the rest:
#   pip install -r requirements-finetune.txt

# HuggingFace stack
transformers>=4.30.0
datasets>=2.0.0

# Evaluation
sacrebleu>=2.3.0
bert-score>=0.3.13

# Data
pandas>=2.0.0
numpy>=1.24.0

# Chinese NLP (shared with requirements.txt)
jieba
pypinyin
opencc
dragonmapper
